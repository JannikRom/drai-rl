# ===============================================================
# TD3 — Template
# Twin Delayed Deep Deterministic Policy Gradient
# ===============================================================

# ---------------------------------------------------------------
# Experiment
# ---------------------------------------------------------------
experiment_name: "td3_pendulum_white_per"
seed: 3

# ---------------------------------------------------------------
# Environment
# ---------------------------------------------------------------
agent_type: "td3"
env_name: "Pendulum-v1"
# if Hockey-v0:
# mode: "NORMAL"          # "NORMAL", "TRAIN_SHOOTING", "TRAIN_DEFENSE"
# opponent: "weak"        # "weak", "strong", null for selfplay

# ---------------------------------------------------------------
# Agent
# ---------------------------------------------------------------
gamma: 0.98
tau: 0.005

# ---------------------------------------------------------------
# Network Architecture
# ---------------------------------------------------------------
actor_hidden_sizes: [128, 128]
critic_hidden_sizes: [128, 128, 128]

# ---------------------------------------------------------------
# Learning Rates
# ---------------------------------------------------------------
actor_lr: 3.0e-4
critic_lr: 3.0e-4

# ---------------------------------------------------------------
# TD3 Specific
# ---------------------------------------------------------------
policy_noise: 0.2 # Gaussian noise added to target policy actions
noise_clip: 0.5 # Clipping range for target policy noise
policy_delay: 2 # Actor update frequency (every N critic updates)

# Exploration noise
noise_type: "white" # "white" or "pink"
noise_scale: 0.1
# if pink noise:
#pink_beta: 1.0

# ---------------------------------------------------------------
# Replay Buffer
# ---------------------------------------------------------------
replay_capacity: 100_000
buffer_type: "per" # "er" or "per"
#if PER:
per_alpha: 0.4
per_epsilon: 1.0e-8
per_beta_start: 0.6
per_annealing_pct: 0.8

# ---------------------------------------------------------------
# Training
# ---------------------------------------------------------------
training_mode: "standard" # "standard" or "selfplay"
total_timesteps: 200_000
learning_starts: 1_000
batch_size: 256

# if selfplay:
# pool_max_size: 20
# pool_update_interval: 500
# pool_update_check_window: 100
# pool_update_eval_episodes: 10
# pool_update_win_rate_threshold: 0.55
# pool_strong_bot_prob: 0.7
# pool_snapshot_prob: 0.5
#pool_recency_bias: 2

# ---------------------------------------------------------------
# Saving
# ---------------------------------------------------------------
log_dir: "./logs/report_3_1/pendulum"
save_interval: 100_000
eval_interval: 500
eval_episodes: 10

# ---------------------------------------------------------------
# Logging
# ---------------------------------------------------------------
logging:
  # TensorBoard scalar intervals (in timesteps)
  loss_every_steps: 100
  per_beta_every_steps: 100

  # Console print interval (in episodes)
  print_every_episodes: 10

  # Moving-average window for console + TensorBoard
  avg_window: 10

  # Which scalar groups to write to TensorBoard
  log_losses: true
  log_per: false # set true if buffer_type: "per"
  log_sac: false # TD3 has no SAC scalars
  log_eval: true
  log_pool: false # set true if training_mode: "selfplay"

  # Artefacts saved at end of training
  save_reward_plot: true
  save_numpy_arrays: true
  save_config_yaml: true
